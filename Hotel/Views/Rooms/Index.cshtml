@model IEnumerable<Hotel.Models.ViewModel.RoomsVM>

@{
    ViewBag.Title = "Index";
}



<p>
    @Html.ActionLink("Create New", "CreateRooms")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BedsId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ServicesId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Size)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Capacity)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.BedsId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ServicesId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Price)
    </td>
    <td>
        @Html.ActionLink("Edit", "EditRooms", new { id = item.Id })
        @Html.ActionLink("Delete", "DeleteRooms", new { id = item.Id })
    </td>
</tr>
}

</table>
